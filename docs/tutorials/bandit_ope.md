# Bandit OPE Tutorial

This tutorial runs the minimal bandit OPE example.

```bash
python examples/minimal/bandit_ope.py
```

The script creates a synthetic bandit dataset, defines a policy value estimand,
and runs IS and WIS with diagnostics.
